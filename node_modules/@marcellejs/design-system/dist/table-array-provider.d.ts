import type { TableProviderOptions } from './table-abstract-provider';
import { TableDataProvider } from './table-abstract-provider';
export declare class TableArrayProvider<T extends Record<string, unknown>> extends TableDataProvider<T> {
    rawData: Array<T>;
    private currentPage;
    constructor({ data, ...options }: TableProviderOptions & {
        data: Array<T>;
    });
    update(): Promise<void>;
    page(i: number): Promise<void>;
    sort(sorting: {
        col: string;
        ascending: boolean;
    }): Promise<void>;
    delete(i: number): Promise<T>;
}
